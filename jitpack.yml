jdk:
  - openjdk21
before_install:
  - echo "Before Install"
  - bash ensure-java-21 install
install:
  - ./gradlew shadowJar
script:
  - ls -la build/libs
  - ls -la
  - cp build/libs/*.jar .
  - ls -la
